apiVersion: v1
kind: Service
metadata:
  name: {{ include "fr-chart.fullname" . }}
  labels:
    {{- include "fr-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fr-chart.selectorLabels" . | nindent 4 }}
